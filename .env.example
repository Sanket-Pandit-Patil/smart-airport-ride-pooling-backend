# Configuration for Docker Compose setup
PORT=3000
NODE_ENV=development
# When running with Docker, the PG_HOST in docker-compose.yml overrides this to 'db'
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=airport_pooling
PG_USER=postgres
PG_PASSWORD=postgres
PG_POOL_SIZE=20
MAX_DETOUR_FACTOR=0.3
BASE_FARE_PER_KM=2.5
POOLING_DISCOUNT_FACTOR=0.85
